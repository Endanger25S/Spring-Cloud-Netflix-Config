spring:
  application:
    name: liming-spring-cloud-netflix-web-admin-feign--
  thymeleaf:
    cache: false
    mode: LEGACYHTML5
    encoding: UTF-8
    servlet:
      content-type: text/html

server:
  port: 10005

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10001/eureka/